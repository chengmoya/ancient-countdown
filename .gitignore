# ==== 个人数据,绝不入库(含邮箱 / 授权码 / 履历) ====
notify_secret.json
notify_state.json
countdown_config.json
自检结果.txt
发信错误日志.txt

# ==== Python 缓存 / 编译产物 ====
__pycache__/
*.pywc
*.pyc

# ==== 打包中间产物 ====
构建中间产物_*/
spec_*/
build/
dist/
上一版*.exe

# ==== 发布产物改走 GitHub Releases,不再入库 ====
发布/古风倒计时.exe
发布/古风倒计时_发布包.zip
# 版本账本:每次打包都会重写(含 exe 生成时间),追踪它只会产生无意义 diff。
# 版本记录的正规去处是 归档/清单.json。
发布/版本.txt

# ==== 版本归档:zip 只留本机,清单与说明入库 ====
# 归档/*/ 下面每个版本一份原样发布包(各约 11 MB)。不入库,否则仓库会被撑爆;
# 清单.json 和 README.md 要入库 —— 换台机器也知道各版长什么样、去哪补。
归档/*/*.zip
