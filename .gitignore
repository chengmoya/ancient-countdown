# ==== 个人数据,绝不入库(含邮箱 / 授权码 / 履历) ====
notify_secret.json
notify_state.json
countdown_config.json
自检结果.txt
测试_*_结果.txt
发信错误日志.txt
# 共用设置.json：为了多台机器同步配置而导出的中转文件。
# 它**明文带着 SMTP 授权码**，敏感度和令牌一样，绝不能进公开仓库。
# 它本来就是个「放到 exe 旁边、启动即被读走删掉」的临时件，不属于仓库内容。
共用设置.json

# ==== Python 缓存 / 编译产物 ====
__pycache__/
*.pywc
*.pyc

# ==== 打包中间产物 ====
构建中间产物_*/
spec_*/
build/
dist/
上一版*.exe
上一版*.zip

# ==== 发布产物改走 GitHub Releases,不再入库 ====
发布/古风倒计时.exe
发布/古风倒计时_发布包.zip
# 版本账本:每次打包都会重写(含 exe 生成时间),追踪它只会产生无意义 diff。
# 版本记录的正规去处是 归档/vX.Y/ 里的原样包(只在本机)。
发布/版本.txt

# ==== 版本归档:只留本机 ====
# 归档/*/ 下面每个版本一份原样发布包(zip)+ 一份未压缩的 exe(双击即那一版,
# 用来试"从这个版本升上来行不行")。各约 11 MB,不入库,否则仓库会被撑爆。
归档/*/*.zip
归档/*/*.exe

# ==== 内部文件:只留本机,绝不入库 ====
# 仓库是公开的。下面这些记录运维细节 / 本机路径 / 开发过程的文档与脚本一律不入库。
# 2026-09-19 发现它们曾被提交进公开仓库(对话内容与发版细节外泄),已改写历史抹除。
# 以后新增同类文档,记得同时补到这一节 —— 别指望下次还会发现。
交接说明.md
项目概要说明.md
在线升级功能评估.md
归档/README.md
归档/清单.json
发版.py
打包.py
自检.pyw
测试_升级模块.py
测试_升级端到端.py
测试_发版工具.py
测试_自动更新.py
测试_配置同步.py
