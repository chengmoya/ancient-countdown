# 古风倒计时 · 使用说明

一个常驻桌面的倒计时小工具。宣纸底、古铜框、朱砂印，剩余时间越少，颜色越紧迫。

---

## 一、文件清单

**如果你拿到的是打包版（一个 exe），只需要 `古风倒计时.exe` 一个文件**，
双击就能用，不需要装 Python。首次启动慢两三秒是正常的（它要先自解压）。

**如果要看/改配置**，第一次运行之后，exe 旁边会自动出现这些：

| 文件 | 作用 |
|---|---|
| `古风倒计时.exe` | **双击这个启动** |
| `mailer.pyw` | 传书使者 —— 发邮件用的独立小程序，被主程序自动唤起，你不用管它 |
| `countdown_config.json` | 配置文件，时间、颜色规则、传书设置都在这里 |
| `notify_secret.json` | **你的邮箱授权码**（程序自动生成，别发给别人） |
| `notify_state.json` | 传书履历（程序自动维护，你不用管） |

**如果你拿到的是源码版**，多这几个：

| 文件 | 作用 |
|---|---|
| `AncientCountdown.pyw` | 主程序（不要直接改） |
| `启动倒计时.bat` | **双击这个启动** |
| `自检.pyw` | 自检工具，排查问题时用（运行后结果写在 `自检结果.txt`） |
| `打包.py` | 需要重新生成 exe 时运行它 |
| `预览图/` | 各个状态的效果截图 |

> 为什么授权码要单独放一个文件？因为 `countdown_config.json` 是你可以随手改、
> 甚至发给别人参考的配置；授权码混在里面，一不留神就泄露出去了。

---

## 二、怎么启动

**打包版**：双击 `古风倒计时.exe`。

**源码版**：双击 `启动倒计时.bat`。

两种方式都没有黑窗口残留，程序直接出现在屏幕右上方。

> 程序运行时不占用任务栏（它是"桌面挂件"形态）。
> 想退出：**在窗口上点右键 → 退出**。

### 开机自动运行（可选）

1. 按 `Win + R`，输入 `shell:startup`，回车
2. 把 `古风倒计时.exe`（或 `启动倒计时.bat`）的**快捷方式**拖进打开的文件夹

### 打包版和源码版的差别

| | 打包版 exe | 源码版 |
|---|---|---|
| 需要装 Python | 不用 | 要（3.9 以上） |
| 启动速度 | 慢 2~3 秒（自解压） | 快 |
| 体积 | 约 11 MB | 几十 KB |
| 发邮件 | 靠系统里的 Python；**没装 Python 时会自动改用 exe 自己发** | 直接用系统 Python |

> 发邮件那栏的意思是：打包版会优先找系统里的 Python 来发信（快），
> 找不到就退回来用 exe 自己兼任 —— 慢一点，但**一定发得出去**。
> 所以就算你在一台没装过 Python 的电脑上，传书功能也照常工作。

---

## 三、设置倒计时

有 3 个入口，任选其一：

- **双击窗口**
- 点击右上角的 **「設」** 字
- 在窗口上**右键 → 设置**

### 时间格式

在「目标时刻」里输入，顺序是 **月 → 日 → 时 → 分**：

| 输入 | 含义 |
|---|---|
| `10.1.11.30` | 10 月 1 日 11:30 |
| `10.1` | 10 月 1 日 00:00 |
| `12.31.23.59` | 12 月 31 日 23:59 |
| `10-1 11:30` | 同上（横线、空格、冒号都行） |
| `10月1日11:30` | 中文写法也可以 |
| `2027.3.5.8.0` | 指定年份 |

**数字中间随便填什么都行。** 这句是字面意思 —— 点了、逗号、顿号、斜杠、
竖线、加号、下划线、空格、制表符……程序只认数字，别的统统当间隔：

| 输入 | 解析结果 |
|---|---|
| `10.1/2，32` | 10 月 1 日 02:32 |
| `10.1/2, 32` | 同上 |
| `10  1  2  32` | 同上 |
| `10|1|11|30` | 10 月 1 日 11:30 |
| `2026/10/1` | 2026 年 10 月 1 日 |

时和分可以省 —— 只写 `10.1 8` 就是 8 点整。

输入框下方会**实时预览**解析结果和剩余时间，绿色 ✓ 表示格式正确。

**写错了会明确告诉你错在哪。** 比如输入 `13.1` 会提示
「月份需在 1 ~ 12 之间，你写的是 13」，而不是默默猜成 2013 年 1 月 1 日。
宁可报错，也不给一个看着像对的结果。

### 关于年份（重要）

**不写年份时，程序自动选"下一个还没到的那个日期"。**

举例：今天是 9 月 17 日，你输入 `10.1`，指向今年 10 月 1 日；
但如果你输入 `1.1`，因为今年 1 月 1 日已经过去了，程序会自动理解成**明年** 1 月 1 日。

想精确指定就写完整年份，例如 `2027.1.1.0.0`。

**两位年份只在「离今年 8 年以内」才认。** 写 `26.10.1` 会读成 2026 年 10 月 1 日；
但 `13.1` 不会被读成 2013 年 —— 那是十六年前了，程序会当你是在打月份，然后报错。
这样做是为了避免「写错一个数字，程序给你一个安静的错误答案」。

---

## 四、颜色规则

规则的含义是：**剩余时间 ≤ 阈值时，切换到对应颜色**。

程序会自动挑出**最紧迫**的那一条，所以列表顺序无关紧要。

默认配了 4 档：

| 剩余时间 | 字色 | 底色 | 效果 |
|---|---|---|---|
| ≤ 7 天 | 竹青 | 宣纸 | 渐渐临近 |
| ≤ 4 天 | 琥珀 | 宣纸 | 开始醒目 |
| ≤ 2 天 | 朱砂 | 宣纸 | 鲜明警示 |
| ≤ 12 小时 | 月白 | **墨黑** | 入夜，白字浮现 |

> **为什么最后半日要连底色一起换？**
> 月白色的字配在米色宣纸上会看不见。所以这一档同时把底色换成墨黑，
> 形成"入夜"的效果——既有戏剧张力，也真的看得清。
> 底色那一栏留空（显示「自」）就表示"保持宣纸底"。

### 怎么改

在设置面板点色块 → 弹出古风色板，12 种预设色任选；点「使用当前底色」可把底色恢复成"自动"。

也支持直接改 `countdown_config.json`：

```json
"thresholds": [
  { "days": 7.0, "color": "#4F7A52", "bg": null,      "label": "剩余 7 日内" },
  { "days": 0.5, "color": "#F2EFE4", "bg": "#1C1A17", "label": "最后 12 小时" }
]
```

`days` 支持小数（`0.5` = 12 小时，`0.02` ≈ 半小时）。
改完存盘，**重启程序**生效。

---

## 五、窗口操作

| 操作 | 效果 |
|---|---|
| 拖动窗口内部 | 移动窗口 |
| 拖动窗口边缘（8 个方向） | 自由缩放，数字字号自动适配 |
| 右键 → 锁定窗口尺寸 | 锁死后只可移动、不可缩放 |
| 右键 → 窗口总在最前 | 开关置顶（默认开） |
| 右键 → 恢复默认大小 | 回到 620 × 300 |
| 右键 → 拉回屏幕 | 窗口找不到时，把它挪回右上角 |
| 右键 → 传书记录 | 看邮件提醒寄出去没有（详见第七章） |
| 双击 / 点「設」 | 打开设置 |
| 在设置面板按 `Esc` | 关闭设置 |

窗口位置和大小会**自动记住**，下次启动保持原样。

> 窗口不会被拖出屏幕——拖到边缘就停住了，这是防止它"丢失"。

---

## 六、归零时

归零后显示楷体大字 **「時辰已到」**，底色转为墨黑，字色在朱砂红和鎏金之间缓慢交替闪烁。

文字和颜色都能在配置里改：

```json
"zero_text": "時辰已到",
"zero_color": "#B03A2E",
"zero_color_alt": "#D9A13B"
```

---

## 七、传书 · 邮件提醒

除了在屏幕上变色，程序还能在剩余时间跨过某一档时**寄一封邮件给你**。
适合"窗口被挡住了 / 人不在电脑前 / 想顺便在手机上看到"这些场合。

### 7.1 开启步骤

**第一步：拿到邮箱授权码**

授权码**不是**你的登录密码。以 QQ 邮箱为例：

1. 浏览器登录 <https://mail.qq.com>
2. 点顶部的 **设置** → **账户**
3. 找到 **POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV服务** 那一段
4. 把 **IMAP/SMTP服务** 右边的开关打开
5. 按提示用手机发一条短信完成验证
6. 验证通过后会弹出一个 **16 位授权码**，复制它

> 163 / 126 邮箱路径类似：设置 → POP3/SMTP/IMAP → 开启 SMTP 服务 → 拿授权码。
> 授权码可以随时作废重领，比密码安全。

**第二步：填进设置面板**

双击窗口 →「传书 · 邮件提醒」区：

| 栏目 | 填什么 |
|---|---|
| 启用邮件提醒 | 打勾 |
| 邮箱服务 | 选你的邮箱；用别家就选「自定义」再填服务器地址 |
| 发件邮箱 | 完整邮箱地址，如 `cheng@qq.com` |
| 授权码 | 刚才那 16 位 |
| 收件邮箱 | 想发到哪；**留空就是发给自己** |
| 本机抬头 | 用来分辨是哪台电脑寄的，见下方说明 |
| 提醒档位 | 剩余 ≤ 多少天时寄一封，可增可删 |

**关于「本机抬头」**

多台电脑都装了这个小工具、又都往同一个邮箱寄信时，收件箱里会全是长一样的邮件，
分不清哪封来自哪台机器。填一个抬头就能区分开，它会同时出现在**三处**：

| 出现的地方 | 长这样 |
|---|---|
| 发件人显示名 | `书房台机 <cheng@qq.com>` |
| 主题前缀 | `【书房台机】倒计时 · 还剩 1 天 23 时（剩两日）` |
| 正文落款 | `—— 书房台机 · 古风倒计时自动传书` |

前两处**不用点开邮件**就能看见——这正是抬头要解决的问题，所以它被放在主题最前面
（收件箱会把长标题截断，而"哪台机器寄的"恰恰最不该被截掉）。

- **默认自动取计算机名**（例如识别为「DESKTOP-ABC123」这样的机器名），所以你不填也能区分；
  改成「书房台机」「办公室那台」这样好记的更好用。
- **留空就不加抬头**，主题回到 `【倒计时】…` 的老样子。
- 最长 24 字；太长会把主题挤爆，保存时会提示。

> **发件人显示名可能被邮箱盖掉。**
> 有些邮箱（QQ 邮箱就是这样）在显示「自己域内」的发件人时，会优先用账号昵称，
> 于是第一列的显示名看起来还是你的 QQ 昵称 —— 这不是程序没发对，
> 邮件头里写的确实是抬头，是收件端换了显示方式。
> **主题最前面的抬头不受影响**，所以判断哪台机器寄的，认主题那一行最稳。

**第三步：点「试寄一封」验证**

不必干等到真的临近两天——这个按钮会立刻寄一封测试信，几秒后给出结果：

| 提示 | 含义 |
|---|---|
| `✓ 已寄出，去收件箱看看` | 配置正确 |
| `✕ 登录被拒 —— 多半填成了登录密码…` | 授权码不对 |
| `✕ 连接邮件服务器超时…` | 网络不通或端口被拦 |
| `✕ 加密方式不匹配…` | 端口和加密方式对不上 |

试寄的信和将来正式寄的信**格式完全一样**（同样的抬头、同样的落款），
只在主题里标了「试寄一封」、正文开头多了两句说明。所以试寄看到什么样，
将来收到的就是什么样 —— 抬头能不能区分机器，试寄这一封就能确认。

**看到「✓」之后再点「保存」。**

### 7.2 什么时候会寄

默认三档，和屏幕上的配色规则一一对应（以抬头「书房台机」为例）：

| 剩余时间 | 主题 |
|---|---|
| ≤ 2 天 | `【书房台机】倒计时 · 还剩 …（剩两日）` |
| ≤ 12 小时 | `【书房台机】倒计时 · 还剩 …（最后半日）` |
| 归零 | `【书房台机】倒计时 · 时辰已到` |

档位随便加，比如改成 7 天 / 3 天 / 1 天。抬头留空时，主题就是原来的
`【倒计时】还剩 …（剩两日）`。

### 7.3 几条刻意设计的行为

**只寄最紧迫的那一封。** 关机三天后才开机，程序发现三档全越过了——
它只补寄**最贴切的那一封**，不会一口气轰三封。其余档位记为「已错过」。

**同一档只提醒一次。** 重启程序不会重发。但你**改了目标时刻，履历就清零**——
那是另一件事了。

**失败会退避重试。** 网络断了那次不白费：10 分钟后重试，最多 3 次；
超过就放弃，并在传书记录里留下失败原因。

**关机期间错过的，开机补上。** 程序启动时立刻核对一次履历，越过了却没寄过的，马上补寄。

### 7.4 查看寄出情况

**在窗口上右键 → 传书记录**：

| 状态 | 含义 |
|---|---|
| 已寄出 | 对方服务器收下了 |
| 寄送中… | 正在发 |
| 寄送失败 | 附失败原因，稍后会自动重试 |
| 已错过 | 这一档被跳过（当时已经有更紧迫的了） |
| 尚未触发 | 还没到这一档 |

### 7.5 关于占用（重要）

**加了邮件提醒，常驻占用一点没变。**

这不是"应该不会"，是设计上就不允许它变：

- 发信这件事**不在主程序里做**。要发的时候，主程序临时唤起 `mailer.pyw`
  这个独立小程序去发，发完它自己退出。
- 主程序全程**不加载**发邮件所需的那几个库。实测主程序的模块表里
  没有 `smtplib`、`email`、`ssl`——它们压根没进内存。

具体数字见下一章。

---

## 八、性能实测

本机（Windows / 16 逻辑核心 / Python 3.14 + Tk 8.6）实测数据。

### CPU

| 场景 | 占用（占全部核心） |
|---|---|
| 常态 620 × 300 | **0.014 %** |
| 放大到 1440 × 810 | **0.016 %** |
| 单次秒刷新耗时 | **2.2 ms** |
| 双击到窗口出现 | **272 ms** |

换个直观的说法：**它每秒只干 2 毫秒的活，剩下 998 毫秒都在睡觉。**
连续采样 35 秒，累计只花掉 78 毫秒 CPU 时间——你在任务管理器里基本看不到它。

窗口放大到 6 倍面积，每次刷新也只从 2.2 ms 变成 2.5 ms。因为每秒需要重绘的**只有那几个数字**，
宣纸纹理、边框、印章这些只在「窗口尺寸变化」或「配色切换」时才画一次。

### 显卡

| 指标 | 结果 |
|---|---|
| GPU（显示卡）占用 | **0 %** |
| 加载的 3D 图形库 | **无** |

它画图用的是 Windows 最传统的 GDI 软件绘制，**完全不调用显卡**。
这一点是从两个角度验证的：

1. 系统的 GPU 引擎计数器里，这个进程**一个样本都没有**；
2. 它加载的 58 个模块里，没有任何 D3D / DXGI / OpenGL / Vulkan 库，
   只有 `gdi32`、`user32`、`tk86t`、`tcl86t` 这些传统组件。

为了确认不是"计数器坏了"，同一时刻抓了整机显卡占用做对照：TslGame 53 %、steam 11 %、
dwm 2 %——计数器工作正常，而倒计时始终是 0。

> 补充一句：把窗口"贴"到屏幕上这个动作由系统的桌面窗口管理器（`dwm.exe`）完成，
> 这份开销算在系统头上，和记事本、资源管理器完全一样，不属于这个程序的成本。

### 内存与磁盘

| 指标 | 数值 |
|---|---|
| 工作集 | **38 ~ 41 MB** |
| 私有 | **22 MB** |
| 连续 35 秒增长 | **0 MB** |
| 磁盘读写（35 秒） | **读 0 KB / 写 0 KB** |

工作集这个数字会随系统内存压力浮动——系统会自动回收不活跃的内存页，
所以不同时刻看到的值不一样，这是正常的。磁盘只在你在设置里改动时才写一次配置。

这部分内存主要是 Python 解释器 + Tk 图形库的固定开销，程序自身的逻辑占比很小。

### 和其他方案比

| 方案 | 内存 | 取舍 |
|---|---|---|
| 本程序（Python + Tk） | ~38 MB | 零依赖，双击即用，随时能改 |
| Electron 类桌面应用 | 150 MB+ | 内置整个浏览器内核 |
| 原生编译（C# / Rust） | 10 ~ 20 MB | 需另装开发环境，改一次编译一次 |

如果你对"绝对最小占用"有执念，第三条路能再省 20 MB 左右，
代价是以后每次想调个颜色都要重新编译一次。以这个工具的体量，不太值。

---

## 九、常见问题

**Q：双击 bat 没反应？**
A：先排除"其实起来了但看不见"——按 `Ctrl + Shift + Esc` 打开任务管理器，
在「详细信息」里找 `pythonw.exe`：

- **有** → 程序已经跑起来了，只是窗口位置不对。往下看第二条。
- **没有** → 多半是没装 Python。去 <https://www.python.org/downloads/> 装 3.10 以上版本，
  安装时务必勾选 **Add python.exe to PATH**。

**Q：程序跑到屏幕外面了 / 找不到了？**
A：三个办法，任选一个：

1. 能看到窗口一角的话：在窗口上**右键 → 拉回屏幕**
2. 完全看不见的话：**再双击一次 `启动倒计时.bat`** —— 它不会重复启动，
   而是把已经在跑的那个窗口拉回右上角
3. 都失效时：删掉 `countdown_config.json` 里的 `window` 那一段，重启程序

> 程序每次启动都会自查位置：只要发现窗口落在屏幕外，就自动拉回默认位置。
> 所以正常情况下这个情况不会再出现了。

**Q：我连点了好几下 bat，会不会开了很多个？**
A：不会。程序是单实例的——已经有一个在跑时，第二次双击只会让那个窗口显形，
不会再占一份内存。

**Q：任务管理器里有一堆 pythonw.exe？**
A：那是重复启动的残留（旧版本没有单实例保护）。全部结束掉，
再双击一次 `启动倒计时.bat` 即可，新版不会再重复开。

**Q：改了配置没生效？**
A：配置在启动时读取。改完 JSON 后需要**退出程序再重新打开**（在设置面板里改的则立即生效）。

**Q：为什么字体不是我想的那种书法体？**
A：程序优先用「楷体」，系统没有就依次退到「仿宋」「宋体」。想换字体可以改 `AncientCountdown.pyw` 里的 `_build_fonts` 方法。

**Q：程序没有出现在任务栏里，怎么切换过去？**
A：这是刻意的"桌面挂件"设计。窗口默认置顶，一般不会被挡住。真要找它，看屏幕右上角。

**Q：试寄总失败，提示「登录被拒」？**
A：九成是填了登录密码。要填的是邮箱**授权码**——在邮箱网页版的设置里申请，16 位。
顺便确认「发件邮箱」写的是完整地址（`cheng@qq.com`），不是 `cheng`。

**Q：设置里显示已寄出，但收件箱没有？**
A：先翻**垃圾箱**。`古风倒计时` 这个发件人没有历史信誉，容易被判成广告邮件。
把它标记一次「非垃圾邮件」，以后就正常了。

**Q：电脑关机期间会发邮件吗？**
A：不会。程序没在跑就寄不出去——这是所有桌面小工具的通病，绕不过去。
但**开机后会自动补寄**：程序启动时核对一遍履历，发现越过了却从没寄出的档位，立刻补一封。

**Q：为什么一次跨过两档，却只收到一封邮件？**
A：刻意的。关机三天后开机时三档全越过了，只寄**最贴切的那封**
（信里写的是当下的真实剩余时间），其余记为「已错过」。连发三封就成了骚扰。

---

## 十、想发给别人用？

对方电脑必须装 Python 才能运行。如果想做成**免安装的单一 exe**，可以打包：

```bat
pip install pyinstaller
pyinstaller --noconsole --onefile --name 古风倒计时 AncientCountdown.pyw
```

生成的 exe 在 `dist/` 里，约 10~15 MB。需要的话我可以帮你打包并测好启动速度。
